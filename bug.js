This bug occurs when using the Expo `Image` component with a URI that contains special characters, particularly spaces.  The image fails to load, and no error is thrown. The URI is correctly formatted and accessible via a browser.